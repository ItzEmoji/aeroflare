name = "aeroflare-proxy"
main = "worker.js"
compatibility_date = "2026-06-20"

# You can configure the following environment variables.
# The values below are the defaults used by the script if not specified.
[vars]
NIXCACHE_REPO = "itzemoji2/nix-cache-7"
NIXCACHE_REGISTRY = "ghcr.io"
NIXCACHE_UPSTREAM = "https://cache.nixos.org"
# NIXCACHE_INDEX_TTL = "<NIXCACHE_INDEX_TTL"
